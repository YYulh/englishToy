<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="word">
	<select id="selectOneE" resultType="String">
		select ediction_word from
			(select * from ediction order by DBMS_RANDOM.RANDOM)
		where rownum = 1
	</select>
	
	<select id="selectOneK" resultType="String">
		select kdiction_word from
			(select * from kdiction order by DBMS_RANDOM.RANDOM)
		where rownum = 1
	</select>
	
	
	
</mapper>












